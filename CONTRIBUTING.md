# Contributing

## Development

Install dependencies:

```bash
npm install
```

Run tests:

```bash
npm test -- --run
```

Build the package:

```bash
npm run build
```

## Pull Requests

- Keep changes scoped and documented.
- Add or update tests for behavior changes.
- Ensure `npm test -- --run` and `npm run build` pass before opening a PR.
